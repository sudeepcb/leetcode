

function replaceElements(arrayElm){
    var array = [];
    var currentGreatest = arrayElm[1];
    for (let index = 0; index < array.length; index++) {
        if(arrayElm[i + 1] > currentGreatest){
            currentGreatest = arrayElm[i + 1];
            array[i] = currentGreatest;
        }
        
        if(currentGreatest > arrayElm[i]){
            
        }

    }
}

//replace elements with greatest element on right side