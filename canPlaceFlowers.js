var canPlaceFlowers = function (flowerbed, n) {
  var count = 0;
  for (var i = 0; i < flowerbed.length; i++) {
    if(flowerbed[i] == 0){
        count++;
    }   
  }

  return flower === 0 ? true : false;
};

canPlaceFlowers([0, 0, 1, 0, 0, 0, 0, 1], 1);
